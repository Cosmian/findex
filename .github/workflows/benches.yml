---
name: Benches

on: workflow_dispatch

jobs:
  bench:
    uses: Cosmian/reusable_workflows/.github/workflows/cargo-bench.yml@benches_with_redis
    with:
      toolchain: stable
      features: test-utils,redis-mem,sqlite-mem,rust-mem
      force: true
