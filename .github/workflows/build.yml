---
name: Cloudproof build

on:
  push:
    tags:
      - '*'
  pull_request:

jobs:
  cloudproof:
    uses: Cosmian/reusable_workflows/.github/workflows/cloudproof.yml@findex_2.0_cover_crypt_8.0
    with:
      project-name: findex
      toolchain: nightly-2023-03-20
      kms-version: 4.0.1
      regression-filename: sqlite.db
      branch-java: callback_exceptions
      branch-js: main
      branch-flutter: callback_exceptions
      branch-python: main
      bench-features: sqlite
    secrets: inherit
